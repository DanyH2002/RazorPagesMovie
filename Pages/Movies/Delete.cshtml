@page "{id:int}" 
@model RazorPagesMovie.Pages.Movies.DeleteModel

@{
    ViewData["Title"] = "Eliminar película";
}

<div class="alert alert-danger mt-4" role="alert" style="max-width: 500px;">
    <h4 class="alert-heading">¿Seguro que quieres eliminar esta pelicula?🎬 </h4>
    <hr />
    <p><strong>Titulo:</strong> @Model.movie.Title</p>

    <form method="post" class="mt-3">
        <input type="hidden" asp-for="movie.Id" />
        <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
        <a asp-page="Index" class="btn btn-secondary btn-sm ms-2">Cancelar</a>
    </form>
</div>
